type Query {
  problems: [Problem]
}

type Mutation {
  createProblem(args: ProblemDto): Problem
}

input ProblemDto {
  description: String!
  solved: Boolean
}

type Problem {
  description: String,
  solved: Boolean
  userId: User,
  agentId: Agent,
}